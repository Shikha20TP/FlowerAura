<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FlowerAura Admin Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        .logo-container {
            width: 100%;
            text-align: center;
            margin-top: 20px;
            margin-bottom: 20px;
        }

            .logo-container img {
                width: 180px;
                height: auto;
            }



        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }


        body {
            background: linear-gradient(135deg, #ffe6ec, #ffffff);
            color: deeppink;
            display: flex;
        }

        .sidebar {
            width: 250px;
            background: #fff;
            height: 100vh;
            padding: 40px 20px;
            box-shadow: 4px 0 20px rgba(0, 0, 0, 0.05);
            position: fixed;
            left: 0;
            top: 0;
        }

        .brand {
            font-size: 28px;
            font-weight: 700;
            color: #e91e63;
            text-align: center;
            margin-bottom: 50px;
        }

        .sidebar ul {
            list-style: none;
        }

            .sidebar ul li {
                margin-bottom: 20px;
            }

                .sidebar ul li a {
                    display: flex;
                    align-items: center;
                    text-decoration: none;
                    color: #444;
                    font-size: 16px;
                    padding: 12px 16px;
                    border-radius: 12px;
                    transition: 0.3s ease;
                }

                    .sidebar ul li a i {
                        margin-right: 12px;
                        color: #e91e63;
                    }

                    .sidebar ul li a:hover,
                    .sidebar ul li a.active {
                        background: #ffe6ec;
                        color: #e91e63;
                        font-weight: 600;
                    }

        .main {
            margin-left: 250px;
            width: calc(100% - 250px);
            padding: 40px;
        }

        header {
            background: #fff;
            padding: 20px 30px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
        }

        .profile {
            display: flex;
            align-items: center;
        }

            .profile img {
                width: 45px;
                height: 45px;
                border-radius: 50%;
                margin-right: 12px;
            }

        .cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .card {
            background: rgba(255, 255, 255, 0.6);
            backdrop-filter: blur(12px);
            padding: 25px;
            border-radius: 20px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s ease;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

            .card:hover {
                transform: translateY(-6px);
            }

            .card h3 {
                font-size: 26px;
                font-weight: bold;
                color: white;
            }

            .card p {
                font-size: 14px;
                color: #666;
                margin-top: 4px;
            }

            .card i {
                font-size: 36px;
                color: #e91e63;
            }



        .main {
            margin: 0;
            width: 100%;
            padding: 20px;
        }

        }
    </style>
</head>

<body>



    <aside class="sidebar">
        <h2 class="brand">FlowerAura</h2>

        <ul>
            <li><a href="#" class="active"><i class="fas fa-tachometer-alt"></i> Dashboard</a></li>
            <li><a href="/Home/Index"><i class="fas fa-users"></i> Customers</a></li>
            <li><a href="/Feedback/Index"><i class="fas fa-comment-dots"></i> Feedbacks</a></li>
            <li><a href="/Flowers/Index"><i class="fas fa-comment-dots"></i> Flower-List</a></li>
           

            <li><a href="/Flowers/Addflower"><i class="fas fa-plus"></i> Add Flowers</a></li>
            <li>
                <a href="@(Context.Session.GetString("User Email") != null ? "/User/Logout" : "/User/Login")">
                    <i class="fas fa-comment-dots"></i> Logout
                </a>
                @(Context.Session.GetString("User Email") != null ? "Log Out" : "")


                </a>
            </li>
        </ul>
    </aside>
    <div class="logo-container">
        <img src="~/images/logo.png" alt="Logo">
    </div>


    <main class="main">
        <header>
            <h2>Welcome, Admin</h2>
            <div class="profile">
                <img src="~/images/admin.png" alt="Admin Profile">
                <span>Flower Aura</span>
            </div>
        </header>

        <section class="cards">
            <div class="card">
                <div>
                    <h3>@ViewBag.CustomerCount</h3>
                    <p>Total Customers</p>
                </div>
                <i class="fas fa-users"></i>
            </div>
            <div class="card">
                <div>
                    <h3>@ViewBag.FlowerCount</h3>
                    <p>Total Flowers</p>
                </div>
                <i class="fas fa-seedling"></i>
            </div>
            <div class="card">
                <div>
                    <h3>@ViewBag.FeedbackCount</h3>
                    <p>Total Feedbacks</p>
                </div>
                <i class="fas fa-comment-alt"></i>
            </div>
        </section>
    </main>

</body>
</html>